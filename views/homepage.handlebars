{{#each programs as |program| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <img src="/images/{{program.img}}" alt="{{program.name}}">
    <h2>
      <a href="/program/{{program.id}}">{{program.name}}</a>
    </h2>
      </div>
  <div class="col-md-7">
    <p>
      {{program.description}}
    </p>
  </div>
</div>
{{/each}}